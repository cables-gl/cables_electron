name: nightly build

on:
  workflow_dispatch:
  schedule:
    - cron:  '0 3 * * *'

jobs:
  draft_release:
    runs-on: ubuntu-latest
    steps:
      - name: create release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          name: "$(date +'%Y-%m-%d')-nightly"
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: v$(date +'%Y-%m-%d')-nightly
          draft: false
          prerelease: true
      - name: current date
        run: |
          echo "debug one: "$date
          echo "debug two: "$(date +'%Y-%m-%d')
          echo "debug three: "${{ env.date }}
  debug:
    needs: draft_release
    runs-on: ubuntu-latest
    steps:
      - name: current date
        run: |
          echo "debug one: "$date
          echo "debug two: "$(date +'%Y-%m-%d')
          echo "debug three: "${{ env.date }}
